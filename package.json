{
  "name": "terra-ui_com",
  "private": true,
  "scripts": {
    "postinstall": "cd client && yarn",
    "start": "foreman start -f Procfile.dev",
    "test": "rspec && cd client && npm run test"
  }
}
